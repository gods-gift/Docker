FROM debian:bookworm
# Copy external-service directory into the image
COPY ./external-service /
# Install necessary dependencies
RUN apt update && apt install -y python3 python3-flask python3-requests
